%h1 Profiles

= link_to image_tag('icons/add.png')+"New Profile", session_filter(new_profile_path), {class:'button_link'}

= table(@profiles,["Name"]) do |profile,klass|
  %tr(class=klass)
    %td= link_to profile.name, profile