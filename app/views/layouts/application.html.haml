!!! 5
%html
  %head
    %title Connie
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags

    - if content_for? :head
      = yield :head

  %body
    #left-sidebar
      #user
      = render 'shared/con_filter'
      %input.search
      .bar
      = render 'shared/nav'
    #main
      = yield
