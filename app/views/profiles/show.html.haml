%h1#profile_name
  = @profile.name
  .model_tools
    = link_to image_tag("icons/pencil.png"), edit_profile_path(@profile)
    = link_to image_tag("icons/cross.png"), @profile, :method => :delete, :confirm => "Are you sure you want to delete #{@profile.name}?"
