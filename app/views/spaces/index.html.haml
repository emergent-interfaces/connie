.page-header
  %h1
    Spaces
    %small Where events happen
    = link_to "<i class='icon-plus'></i> New Space".html_safe, session_filter(new_space_path), {class:'btn pull-right'}

%h2 Spaces Tree
.spaces
  - spaces = Space.order('lft ASC')
  = nested_li(spaces) do |space|
    .space
      .body= link_to space.name, space