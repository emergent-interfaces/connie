.page-header
  %h1
    Jobs
    %small= "Listing of jobs for #{default_convention_name}"
    = link_to "<i class='icon-plus'></i> New Job".html_safe, session_filter(new_job_path), {class:'btn pull-right'}

= table(@jobs,["Name"]) do |job,klass|
  %tr(class=klass)
    %td= link_to job.name, job
